//@flow
import React from "react";
import clsx from "clsx";
import { compose } from "recompose";

const TmplTypedComponent = compose()(
  /**
   *
   * @param {SSFATFScoreTableProps} props
   * @returns {null}
   * @constructor
   */
  function TmplTypedComponent(props) {
    return null;
  }
);

export default TmplTypedComponent;
