import { ScoringDto } from "types/typings-api";

export interface SSOtherSettingTableProps {
  context?: TableContextProps;
  scoring: ScoringDto;
  style?: any;
}
export default function SSOtherSettingTable(
  props: SSOtherSettingTableProps
): JSX.Element;
